upstream chargebackend {
    server chargebackend:8000;
}
server {
    listen 80;

    location /api/ {
        proxy_pass http://chargebackend;
        proxy_set_header Host $http_host;
    }
 
    
}